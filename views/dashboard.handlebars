{{#each posts as |post| }}
<header class="p-3 mb-4 header ">
    <div class="container">
    <!-- <p class="lead">I am from Mexico.</p> -->
    <div class="d-flex justify-content-between">
        <div class="h2 mr-auto p-2" href="/dashboard/edit/{{id}}">{{title}}</div>
        <div class="lead p-2">Posted on {{format_date date}}</div>            
    </div>
    </div>
 </header>
  {{/each}}
   <div class=" d-flex justify-content-center"> 
        <button class="btn btn-info btn-lg btn-block" id="btn-newPost"> + New Post</button>
    </div>     

   	<script src="/js/dashboard.js"></script>