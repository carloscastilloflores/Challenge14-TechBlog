<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css">
  <title>Document</title>
</head>
<body>
        <nav class="bg-light p-3 px-5"> 
            <div class="d-flex px-5 pt-4 justify-content-end">
                <a class="h5 text-decoration-none link-dark" href="/">Home </a>
                <a class="h5 px-4 text-decoration-none link-dark" href="/dashboard">Dashboard </a>
                <a class="h5 px-4 text-decoration-none link-dark" href="/login">Log In </a>
                {{#if loggedIn}}
                <a class="h5 px-4 text-decoration-none link-dark" id="logout" href="/logout">Log Out </a>
                {{/if}}
            </div>
          
            <div>
                <h1 class="px-5 display-4">The Tech Blog</h1>
            </div>
        </nav>
    {{!-- <header class="p-3 mb-4 header ">
        <div class="container">
        <!-- <p class="lead">I am from Mexico.</p> -->
        <div class="d-flex justify-content-between">
            <div class="h2 mr-auto p-2">Why MVC is so important</div>
            <div class="lead p-2">Posted by CarlosCastillo on 30/06/2023</div>   
       </div>
        <ul class="list-group" >
            <li class="list-group-item p-3">MVC allows developers to maintain a true separation of concerns, devising their code between the Model layer for data, the View layer for design, and the Controller layer for application logic.</li>
        </ul>
        </div> --}}
    </header>
    <main> 
            {{{body}}}
    </main>
    {{#if loggedIn}}
    <script src="./js/logout.js"></script>
    {{/if}}
    <script src="./js/login.js"></script>
</body>
</html>